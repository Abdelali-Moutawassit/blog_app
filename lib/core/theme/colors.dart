import 'package:flutter/material.dart';

class AppColors {
  // 🌕 Couleurs principales
  static const Color primary = Color.fromARGB(232, 217, 167, 2); // Bleu foncé
  static const Color primaryLight = Color(0xFF5472D3); // Bleu clair
  static const Color primaryDark = Color(0xFF002171);

  // 🌑 Couleurs secondaires
  static const Color secondary = Color(0xFFFF6F00); // Orange
  static const Color secondaryLight = Color(0xFFFFA040);
  static const Color secondaryDark = Color(0xFFC43E00);

  // 🧼 Neutres
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
  static const Color lightGrey = Color(0xFFF2F2F2);

  // 🔴 États (Succès / Erreur)
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFF44336);
  static const Color warning = Color(0xFFFFC107);
  static const Color info = Color(0xFF2196F3);

  // 🌀 Arrière-plan
  static const Color background = Color(0xFFF5F5F5);
  static const Color surface = Color(0xFFFFFFFF);

  // ✨ Spéciaux
  static const Color divider = Color(0xFFBDBDBD);
  static const Color shadow = Colors.black26;
}
